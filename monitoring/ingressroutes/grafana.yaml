# =============================================================================
# Traefik IngressRoute exposing grafana at  http://<host>/grafana/
# Backend: monitoring-grafana:80
#
# Apply with:
#   kubectl apply -f monitoring/ingressroutes/grafana.yaml
# =============================================================================
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`a3263741e4836462a93065a499721c91-7b38811c68251c4a.elb.us-east-1.amazonaws.com`) && PathPrefix(`/grafana`)
      kind: Rule
      services:
        - name: monitoring-grafana
          port: 80
